# Site Settings

paginate: 5

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed]
baseurl: /GalaxyToastmaster

#asset 
assets:
    img: /assets/img
    imgbasic: /assets/img/basic

#for develop
#github:
#    url: /GalaxyToastmaster
    
# feed settings
title:		     "{{ site.data.settings.title }} | {{ site.data.settings.tagline }}"

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
feed:
    path:	     rss-feed.xml
